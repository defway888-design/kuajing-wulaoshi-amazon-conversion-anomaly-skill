# 跨境吴老师亚马逊转化率异动分析 Skill

## 一、用途

用于 Amazon商品转化率已经发生上涨或下降后，按固定数据口径核查：

- 自身标题修改、基础价格、Coupon、Deals、Prime价格和当前 Buy Box。
- 自身评分、页面评价数量和新增一至三星负面评价。
- 五个严格相似竞品的标题修改、价格、Coupon、Deals、Prime价格、评分、页面评价数量和新增评价。

Skill会先从目标商品最细叶子类目 BSR 1—100中筛选相似商品，再按卖家精灵预测近30天销量选择五个不同父商品；不足五个时改用最多三个通用核心词的 Amazon自然搜索 Top 10重建候选池。

最终输出可复核的时间证据、数据状态和方向判断。多个证据组同时成立时输出复合影响，不强制给出唯一原因，默认不提供优化建议。

当前版本：`v2.1.0`  
更新日期：`2026-07-29`

启动示例：

```text
使用跨境吴老师亚马逊转化率异动分析 Skill，分析 <ASIN> 在美国站 <异常期> 转化率下降的原因；基准期按默认规则生成。
```

```text
用 $kuajing-wulaoshi-amazon-conversion-anomaly 分析 <ASIN> 在 <异常期> 的转化率上涨，基准期为 <基准期>。
```

## 二、首次安装

1. 登录 GitHub并提交用户名
2. 接受私有仓库邀请
3. 在 Codex中发出安装指令
4. 重启 Codex让 Skill生效

### 1. 登录 GitHub并提交用户名

首次安装前，需要先将自己的 GitHub用户名提交给跨境吴老师。

```text
登录 GitHub
-> 点击右上角头像
-> 选择 Your profile
-> 进入个人主页
-> 复制浏览器地址栏中的完整网址
-> 将网址发送给跨境吴老师
```

注意：

- GitHub用户名不是邮箱。
- 不要发送邮箱密码、GitHub密码、Token或其他授权信息。

### 2. 接受私有仓库邀请

跨境吴老师收到 GitHub用户名后，会发送私有仓库访问邀请。

```text
登录 GitHub
-> 打开 GitHub发送的邀请邮件
-> 点击 View invitation
-> 点击 Accept invitation
```

能够看到仓库页面和文件列表，即表示已经获得访问权限。

### 3. 在 Codex中发出安装指令

打开 Codex，新建一个对话，输入：

```text
请从以下 GitHub私有仓库安装跨境吴老师亚马逊转化率异动分析 Skill：
https://github.com/defway888-design/kuajing-wulaoshi-amazon-conversion-anomaly-skill
```

按照 Codex提示完成 GitHub授权。

### 4. 重启 Codex

安装完成后，关闭并重新打开 Codex，使 Skill生效。

## 三、运行前准备

当前 Codex环境需要配置：

- 领星 MCP：提供当前店铺、站点、自有 Seller ID、当前父子商品关系，以及上游自有转化率口径。
- 西柚 MCP：提供自身及竞品当前标题、标题修改历史和 Prime价格日趋势。
- 卖家精灵 MCP：提供竞品池、当前主图和五点、预测销量、基础价格、Coupon、Deals、评分和评论数据。

运行时至少提供站点、ASIN、异常期和转化率上涨/下降方向。同站点存在多个店铺且异常记录无法确定店铺时，还需要指定店铺。

同一种数据只使用一个固定 MCP来源。字段缺失或调用失败时输出数据不足，不调用其他 MCP补数。

## 四、执行结果

执行后会得到：

1. 当前父子商品范围。
2. 最终竞品及其候选池来源、代表子体和卖家精灵近30天预测销量。
3. 自身与竞品的标题修改记录。
4. 自身与竞品的价格促销证据，包含 Prime价格。
5. 自身与竞品的口碑证据。
6. 当前 Buy Box风险。
7. 支持证据、候选支持证据、反向证据、不支持归因、无法判断和数据不足项。
8. 单一证据组或复合影响诊断。

标题修改、Prime价格和 Deals都不持续监控，只在转化率异动发生后按需读取；已有同源、同站点、同 ASIN、同时间窗口数据时复用。

## 五、仓库文件

- `SKILL.md`：Skill入口、数据源边界、执行流程和输出合同。
- `agents/openai.yaml`：Codex展示名称和默认提示词。
- `references/business-rules.md`：竞品筛选、标题修改、Prime价格、其他价格促销、评分评论、时间证据和归因判断规则。
- `references/mcp-routing.md`：MCP工具、字段、调用顺序、缓存和错误处理。
- `template_manifest.json`：跨境吴老师品牌与版本信息。

## 六、版本更新记录

从本说明文档加入版本记录后，后续每次 GitHub功能更新都必须在这里追加一行，便于用户判断当前Skill是否为最新版本。表中的GitHub提交记录该功能版本对应的主要提交；仅补充说明文档时不提升功能版本，也不新增版本行。

| 版本 | 更新时间 | GitHub提交 | 更新内容 | 影响范围 |
|---|---|---|---|---|
| v2.1.0 | 2026-07-29 | `待提交` | 新增自身与竞品标题修改、Prime价格变化；标题与Prime固定使用西柚按需回溯，并补充单一数据源、时间覆盖、竞品汇总和重复归因规则。 | 自身因素、竞品因素、MCP路由、证据分组、输出合同 |
| v2.0.1 | 2026-07-28 | `7609efb` | 将技术编号改为清晰业务标题；删除本地卖家精灵数据库Skill依赖和固定MCP命名空间限制，使Skill能够独立更新和运行。 | Skill入口、业务判断规则、MCP路由、用户说明 |
| v2.0.0 | 2026-07-25 | `71ed4e3` | 按最终确认的完整业务规则重构竞品生成、近30天预测销量、价格促销、评分评论、时间证据和归因流程。 | 竞品生成、价格促销、评分评论、证据归因 |
| v1.0.0 | 2026-07-21 | `4c20418` | 完成基础版Skill封装、品牌信息、MCP路由、业务规则和私有GitHub安装说明。 | Skill基础包、业务规则、安装说明 |
