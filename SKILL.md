---
name: kuajing-wulaoshi-amazon-conversion-anomaly
description: 跨境吴老师亚马逊商品转化率异动诊断 Skill。用于用户要求分析 Amazon ASIN、父商品或子商品的转化率/CVR/Unit Session Percentage 上涨或下降原因，核查自身价格、Coupon、Deals、Buy Box、Prime 人工项，以及竞品价格促销、评分和评论变化时使用；基于领星与卖家精灵 MCP 输出可复核证据，不强行给出唯一根因。
---

# 跨境吴老师亚马逊转化率异动分析

本 Skill 为跨境吴老师专用模板，未经授权不得移除、替换或弱化 Skill 名称、执行提示和页面标题中的跨境吴老师标识。

## 使用边界

本 Skill 在转化率异动已经被发现后执行，负责解释异动期间发生了什么。它不承担持续监控，不为证据不足的因素补值，也不把时间上不重合的变化写成原因。

输出是证据化诊断，不强制选出唯一根因。多个因素同时发生时，标记为复合事件。除非用户另行要求，不输出优化建议。

## 必读参考

正式分析前必须完整读取：

- `references/business-rules.md`：时间窗口、商品范围、竞品选择、全部因素与判断规则。
- `references/mcp-routing.md`：领星和卖家精灵 MCP 的固定路由、字段、复用、分页与失败处理。

不得只凭本文件执行正式分析。

## 输入合同

至少需要：

- Amazon 站点。
- 一个父 ASIN 或子 ASIN。
- 异常期，以及转化率上涨或下降方向。

如果用户未给基准期，默认取异常期之前、长度相等、互不重叠的连续区间。全部时间按目标站点当地时区计算，内部统一使用左闭右开区间 `[start, end)`。

如果同一站点存在多个自有店铺且用户未指定店铺，必须让用户选择；不得随意使用某个店铺的 Seller ID。若缺少异常期或异动方向，先请求上游异动结果或用户补充，不自行发明阈值。

## 固定执行流程

1. 输出进度提示：`跨境吴老师转化率异动分析：正在确认店铺、站点和商品范围。`
2. 用领星 MCP 在当前用户环境动态取得店铺、站点、Seller ID、父 ASIN和子 ASIN；禁止硬编码测试账号或测试商品。
3. 构建 `child_asin_union`：当前子体、基准期子体、异常期子体的并集。用户或上游已指定异常子体时，以指定子体为分析对象，但并集仍用于排除同父商品子体和补全证据。
4. 为每个异常子 ASIN 自动生成一次竞品集合：按当前图片、标题、卖点和购买用途判断相似性，排除目标商品同一父 ASIN 的所有子体，再按最新完整月子 ASIN 销量取前五名。该集合在本次全部竞品分析中复用。
5. 按 `references/mcp-routing.md` 先做聚合调用，再做 Coupon 与评论调用；复用已经由同一任务的流量异动模块取得的 Deals 数据。
6. 按 `references/business-rules.md` 对每个因素输出 `有变化`、`无变化`、`数据不足` 或 `人工核查`。
7. 将变化事件与异常期做时间匹配，并判断其方向对本次转化率变化是 `支持`、`相反` 或 `未知`。
8. 合并证据组，避免把同一价格促销事件或同一评论事件重复算作多个独立根因。
9. 输出进度提示：`跨境吴老师转化率异动分析：证据核查完成，正在生成结果。`

## 因素范围

自动核查的自身因素：

1. 商品基础价格变化。
2. Coupon 变化。
3. Deals 变化。
4. 当前 Buy Box 是否由自有 Seller ID 持有。

人工核查项：Prime 折扣。该项不得自动调用、自动归因或安排定时抓取。

自动核查的外部因素：

1. 竞品价格变化。
2. 竞品 Coupon 变化。
3. 竞品 Deals 变化。
4. 自身评分变化。
5. 自身评价数量及新增负面评价变化。
6. 竞品评分变化。
7. 竞品评价数量及新增评价变化。

只执行上述范围，不自行增加分析因素。

## 结果要求

每个自动因素必须包含：

- 数据状态。
- 基准期口径和值。
- 异常期口径和值。
- 变化事件与发生时间。
- 是否与异常期重合。
- 对异动方向的解释：支持、相反或未知。
- 可复核的数据来源和关键数值。

竞品因素必须采用“逐个列明 + 数量汇总 + 新增/停止/分化”的结构。数据不足的竞品与成功返回空值的竞品必须分开统计。

最终结论按以下结构输出：

```text
跨境吴老师亚马逊转化率异动分析

分析对象：站点 / 店铺 / 父 ASIN / 异常子 ASIN
时间窗口：基准期 / 异常期 / 站点时区
转化率异动：上涨或下降

一、自身因素
1. 因素名称
   数据状态：有变化/无变化/数据不足/人工核查
   数据证据：基准期、异常期、事件时间、变化量
   时间匹配：重合/不重合/无法判断
   方向判断：支持/相反/未知

二、外部因素
1. 因素名称
   逐个结果：五个竞品分别列明
   数量汇总：上涨/下降/新增/停止/持续/无活动/数据不足
   分化情况：是否存在方向分化
   方向判断：支持/相反/未知

三、综合诊断
- 证据一致的因素
- 方向相反的因素
- 数据不足与人工核查项
- 单因素事件或复合事件
```

证据不足时必须明确写出缺失工具、字段或时间覆盖，不得把“未返回”解释为“没有变化”。

