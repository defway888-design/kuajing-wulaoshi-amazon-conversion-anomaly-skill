---
name: kuajing-wulaoshi-amazon-conversion-anomaly
description: 跨境吴老师亚马逊商品转化率异动诊断 Skill。用于 Amazon ASIN、父商品或异常子商品的转化率/CVR/Unit Session Percentage 上涨或下降归因，自动生成五个严格相似竞品，并核查自身与竞品的标题修改、Prime价格、基础价格、Coupon、Deals、评分、页面评价数量、新增评价以及当前 Buy Box。固定使用领星取得当前自有商品身份，西柚取得标题与Prime历史，卖家精灵取得其他外部与可比趋势；高优先级父 ASIN五点为空时按子 ASIN补抓五点作为辅助证据，输出可复核证据而不强行给出唯一根因。
---

# 跨境吴老师亚马逊转化率异动分析

本 Skill 为跨境吴老师专用模板，未经授权不得移除、替换或弱化 Skill 名称、执行提示和结果标题中的跨境吴老师标识。

## 任务边界

在转化率异动已经被上游或用户确认后执行诊断。只解释基准期和异常期发生了什么，不自行发明异动阈值，不承担持续监控，默认不输出优化建议。

只分析本 Skill 明确列出的因素。变化是证据，不等于确定原因；允许输出复合影响，不强制选择唯一根因。

## 数据源硬规则

同一种数据只能使用一个固定 MCP 来源，禁止多 MCP 交叉验证、补数或替代：

- 领星 MCP：当前店铺、站点、自有 Seller ID、自有父 ASIN与当前子 ASIN关系。
- 西柚 MCP：目标商品与候选商品的当前标题，以及异常子 ASIN和最终竞品的标题修改历史、Prime价格日趋势。
- 卖家精灵 MCP：类目和竞品池、商品当前主图/五点、竞品父子关系、预测销量、基础价格、Coupon、Deals、评分、页面评价数量和评论。
- 上游转化率数据如需读取，只使用领星口径；本 Skill 不使用卖家精灵销量替代真实店铺转化率数据。

任一固定来源不可用或核心字段缺失时，标记对应因素数据不足，不调用其他 MCP兜底。卖家精灵返回的标题不得用于当前标题或标题修改判断，西柚返回的其他价格字段不得替代卖家精灵基础价格与 Deals。

五点属于 Listing 辅助证据，不是核心诊断字段。高优先级 ASIN深度诊断中，如果目标父 ASIN五点为空，且诊断需要 Listing内容或严格相似竞品判断，必须先按 `references/mcp-routing.md` 的子 ASIN补抓流程尝试取得五点；补抓失败只能记录证据缺口，不得阻断诊断报告生成或邮件发送状态。

## 安装与运行环境适配

- 不假设 Skill安装在任何固定盘符、用户目录、Codex目录或工作区。安装位置由执行人员当前客户端、Skill安装器和操作系统决定。
- 运行时以客户端实际加载的 `SKILL.md`所在 Skill根目录为基准解析 `references/business-rules.md`、`references/mcp-routing.md`和其他包内资源；不得拼接作者电脑路径或历史工作区路径。
- 如果当前客户端通过资源加载器而不是文件系统提供 Skill内容，使用加载器提供的相对资源解析能力，不得把资源标识转换成自定义本地绝对路径。
- 不自行复制、移动或重新安装本 Skill，不修改执行人员的 Skill目录、Codex配置、环境变量或 MCP配置文件。
- 领星、西柚和卖家精灵 MCP均在运行时从当前可用工具目录中发现。不得假设固定服务器名、命名空间、URL、端口、密钥保存位置或本地配置文件路径。
- 需要临时文件时，只使用当前平台提供的任务临时目录或当前已授权工作区，并使用任务专用名称；不得写入固定本机目录。当前流程不需要持久化日快照或本地缓存文件。
- 当前环境缺少包内参考文件或必需 MCP时，明确输出缺失项和受影响因素，不得转而搜索作者电脑目录、其他本地 Skill或未授权路径。

## 必读参考

正式执行前完整读取：

- `references/business-rules.md`：竞品筛选、价格促销、口碑变化、时间证据和归因判断的完整规则。
- `references/mcp-routing.md`：固定 MCP 工具、字段、调用顺序、缓存、分页与失败处理。

不得只凭本文件执行正式分析。当前 Skill 已包含所需领星、西柚和卖家精灵工具及字段规则，不依赖其他本地 Skill，也不依赖任何固定本机安装路径。

## 输入合同

至少需要：

- Amazon 站点。
- 一个父 ASIN或异常子 ASIN。
- 异常期。
- 转化率上涨或下降方向。

基准期未指定时，取异常期之前、长度相等、连续且不重叠的区间。统一使用上游转化率模块的 Amazon 站点报告时区，内部使用左闭右开区间 `[start, end)`。

同站点存在多个自有店铺且无法从异常记录确定店铺时，请用户选择。缺少异常期、异动方向或报告时区且无法从上游确定时，停止相关比较并说明数据不足。

## 固定执行流程

1. 输出：`跨境吴老师转化率异动分析：正在确认店铺、站点和当前商品范围。`
2. 用领星取得目标店铺、Seller ID、父 ASIN和当前子 ASIN；主诊断对象只取上游明确标记的异常子 ASIN。
3. 对每个需要独立分析的异常子 ASIN生成一次竞品集合：
   - 先从目标最细叶子类目 BSR 1—100筛选。
   - 用西柚取得当前标题，用卖家精灵取得当前主图和五点；如果高优先级目标父 ASIN五点为空，先按子 ASIN补抓流程取得辅助五点，再严格核对三项内容。
   - 按竞品父 ASIN去重，每个父商品选预测近30天销量最高的代表子体。
   - 按预测近30天销量取五个不同父商品。
   - 不足五个时，放弃整套 BSR 候选池，改用最多三个通用核心词的 Amazon 自然搜索 Top 10结果重建候选池。
4. 竞品集合在本次全部竞品因素中复用，禁止不同因素重新选竞品。
5. 对异常子 ASIN和最终竞品代表子体，用西柚按需取得标题修改历史和 Prime价格日趋势；不持续抓取，不查询无关完整历史。
6. 按 `references/mcp-routing.md` 取得卖家精灵可合并趋势，再取得 Coupon 和评论。Deals 只在异动后按需查询；满足复用键时复用已有结果。
7. 按 `references/business-rules.md` 生成判定方式、业务结果、数据状态、证据时间、时间可信度和方向判断。
8. 合并同一证据组，区分支持证据、候选支持证据、反向证据、不支持归因、无法判断和当前风险。
9. 输出：`跨境吴老师转化率异动分析：证据核查完成，正在生成结果。`

## 因素范围

自身因素：

1. 自身标题修改。
2. 商品基础价格变化。
3. Coupon 变化。
4. Deals 变化，仅异动后按需查询。
5. Prime 价格变化，仅异动后按需查询。
6. 当前 Buy Box归属。
7. 自身评分变化。
8. 自身页面评价数量变化。
9. 自身新增一至三星负面评价变化。

竞品因素：

1. 竞品标题修改。
2. 竞品基础价格变化。
3. 竞品 Coupon 变化。
4. 竞品 Deals 变化。
5. 竞品 Prime 价格变化。
6. 竞品评分变化。
7. 竞品页面评价数量变化。
8. 竞品新增评价与口碑变化。

主图修改和五点修改继续排除。禁止自行增加其他因素。

## 输出合同

固定按以下顺序输出：

```text
跨境吴老师亚马逊转化率异动分析

1. 分析对象、站点、店铺、基准期、异常期、报告时区和转化率方向
2. 当前父子商品范围
3. 五个竞品、候选池来源、代表子体和卖家精灵近30天预测销量
4. 自身标题修改
5. 竞品标题修改
6. 自身价格促销竞争力，包含 Prime价格
7. 竞品价格促销压力，包含 Prime价格
8. 自身口碑变化
9. 竞品口碑变化
10. 当前 Buy Box风险
11. 支持证据、候选支持证据、反向证据、不支持归因、无法判断和数据不足项
12. 综合诊断：单一证据组或复合影响
13. 高优先级 ASIN深度诊断状态：核心诊断状态、可发送状态、证据缺口类型、证据缺口说明和字段映射记录
```

每个因素至少包含：

- 判定方式：期间对比或当前状态。
- 业务结果：具体变化、无变化、当前持有或当前未持有。
- 数据状态：数据完整、数据不足或不适用。
- 基准期值、异常期值、变化量和全部有效事件。
- 证据时间、时间可信度和判断依据。
- 方向判断及所属证据组。
- MCP、工具、ASIN、字段和关键数值。

竞品结果固定使用“逐个列明 + 数量汇总 + 新增/停止/分化”。成功空结果、字段缺失和调用失败必须分开表达。
